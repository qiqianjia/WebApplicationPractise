@model IEnumerable<string>

@Html.ActionLink("Home", "List", "Product")

@foreach (var link in Model)
{
    @*@Html.RouteLink(link,new { controller="Product",action="List",P_Category=link,P_Page=1})*@
    @Html.RouteLink(link,
    new { controller = "Product", action = "List", P_Category = link, P_Page = 1 },
    new { @class=link==ViewBag.SelectedCategory?"selected":null}
    )
}

